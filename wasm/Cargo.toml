[package]
name = "isoquant-wasm"
version = "0.1.0"
edition = "2021"
description = "IsoQuant compression for embeddings - WASM port for Cloudflare Workers"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = 3
lto = true